extends base.pug
include mixins/chatText.pug

block content 
    each chat in chats
        +chatText(chat)
    
    form(method="POST")
        input(name="text", ,required)
        input(type="submit", value="Send")
