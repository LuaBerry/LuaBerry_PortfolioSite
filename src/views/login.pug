extends base.pug

block content
    form(method="post")
        label(for="code") Admin code
        br
        input(name="code", type="password", required)
        br
        input(type="submit", value="Login")